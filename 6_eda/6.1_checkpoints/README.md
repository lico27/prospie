# Checkpoints

This folder (locally) contains two pickle files: `funders_df.pkl` and `grants_df.pkl`. These files contain the dataframes used in the EDA notebook and have been saved as checkpoints before the cleaning and analysis processes take place.