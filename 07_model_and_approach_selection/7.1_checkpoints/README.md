# Checkpoints

This folder (locally) contains three pickle files: `funders_df.pkl`, `recipients_df.pkl` and `embedding_pairs.pkl`. These files contain the dataframes used in the model and approach selection notebooks and have been saved as checkpoints before the experimentation takes place.